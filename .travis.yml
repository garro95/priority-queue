language: rust
rust:
  - stable
  - beta
  - nightly
matrix:
  allow_failures:
    - rust: nightly
script:
  - cargo build --verbose
  - cargo test --verbose
  - RUSTFLAGS="--cfg serde" cargo build --verbose
  - RUSTFLAGS="--cfg serde" cargo test --verbose